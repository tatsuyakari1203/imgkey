# ImgKey compact GPU packaging note.
#
# The optional GPU executable uses native/imgkey_cuda/imgkey_cuda.dll and does
# not require extra Python GPU packages. Keep this file pip-installable as a
# no-op for older build scripts that still run:
#   python -m pip install -r requirements-gpu-runtime-cu128.txt
#
# Build the native DLL before running PyInstaller:
#   native/imgkey_cuda/build.ps1
